{
  "type": "module",
  "scripts": {
    "dev": "vite dev",
    "build": "rm -rf build .svelte-kit && vite build",
    "deploy": "npx gh-pages -d build -t true"
  },
  "devDependencies": {
    "sass": "1.35.1",
    "svelte": "3.55.1",
    "svelte-preprocess": "4.10.6"
  },
  "dependencies": {
    "@sveltejs/adapter-static": "^1.0.5",
    "@sveltejs/kit": "^1.2.2",
    "vite": "^4.0.4"
  }
}